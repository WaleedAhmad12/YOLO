## Image Object Detection

Build and test with `python=3.9`

### Setup
```
# Create Envirnment
python3 -m venv .venv
source .venv/bin/activate

# Install Requirements

pip install -r requirements.txt
```

### Inference

```
python main.py ARGS 
```

* image_path : -----
* --